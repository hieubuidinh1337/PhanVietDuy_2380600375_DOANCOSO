{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Server=JAKIVIP\\SQLEXPRESS;Database=VietduyDB;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=true"
  },

  // ═══════════════════════════════════════════════════════════════
  // HƯỚNG DẪN LẤY GOOGLE CREDENTIALS:
  // 1. Vào https://console.cloud.google.com → Tạo/chọn project
  // 2. APIs & Services → Credentials → Create Credentials → OAuth 2.0 Client ID
  // 3. Application type: Web application
  // 4. Authorized redirect URIs: https://localhost:PORT/signin-google
  // 5. Copy ClientId và ClientSecret vào đây
  // ═══════════════════════════════════════════════════════════════
  "Authentication": {
    "Google": {
        "ClientId": "",
        "ClientSecret": ""
    },
    // ═══════════════════════════════════════════════════════════════
    // HƯỚNG DẪN LẤY FACEBOOK CREDENTIALS:
    // 1. Vào https://developers.facebook.com → My Apps → Create App
    // 2. Chọn "Consumer" → Set Up Facebook Login
    // 3. Settings → Basic: lấy App ID và App Secret
    // 4. Facebook Login → Settings → Valid OAuth Redirect URIs:
    //    https://localhost:PORT/signin-facebook
    // 5. Copy AppId và AppSecret vào đây
    // ═══════════════════════════════════════════════════════════════
    "Facebook": {
      "AppId": "YOUR_FACEBOOK_APP_ID_HERE",
      "AppSecret": "YOUR_FACEBOOK_APP_SECRET_HERE"
    }
  },

  // ═══════════════════════════════════════════════════════════════
  // HƯỚNG DẪN ĐĂNG KÝ VNPAY SANDBOX:
  // 1. Vào https://sandbox.vnpayment.vn/apis/vnpay-demo/
  // 2. Đăng ký tài khoản merchant sandbox (miễn phí)
  // 3. Sau khi có tài khoản, vào Admin Portal lấy:
  //    - TmnCode: mã merchant của bạn (vd: VNPAYTEST)
  //    - HashSecret: khóa bí mật để ký giao dịch
  // 4. Thẻ test VNPay Sandbox:
  //    - Số thẻ: 9704198526191432198
  //    - Ngày hết hạn: 07/15
  //    - Tên chủ thẻ: NGUYEN VAN A
  //    - OTP: 123456
  // ═══════════════════════════════════════════════════════════════
  "VNPay": {
    "TmnCode": "YOUR_VNPAY_TMN_CODE_HERE",
    "HashSecret": "YOUR_VNPAY_HASH_SECRET_HERE",
    "BaseUrl": "https://sandbox.vnpayment.vn/paymentv2/vpcpay.html",
    "ReturnUrl": "https://localhost:7001/checkout/vnpay-return"
  }
}